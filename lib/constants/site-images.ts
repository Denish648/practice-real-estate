/**
 * Fixed artwork for the marketing backgrounds.
 *
 * These are deliberately NOT deal photos — the hero and the closing call to
 * action used to borrow a featured listing's cover, which changed whenever a
 * broker published something new. Deal photos still drive the property list,
 * the spotlight and the FAQ thumbnail.
 *
 * To use your own photo: drop it in `public/images/` and point the constant at
 * it.
 */
export const HERO_IMAGE = "/images/hero-section.png"
export const CONTACT_IMAGE = "/images/contact.svg"
